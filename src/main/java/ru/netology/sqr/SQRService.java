package ru.netology.sqr;

public class SQRService {
    int minRange = 200;
    int maxRange = 300;

    SQRService service = new SQRService();
    system.out.println ("Количество квдратов в диапазоне: " + service.shouldNumberOfSQR(minRange,maxRange));
}
